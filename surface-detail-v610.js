import * as T from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';let q=setInterval(()=>{if(!window.LuxWorld?.roads?.length)return;clearInterval(q);const W=LuxWorld,S=W.scene;function tex(base,noise=16,size=256){let c=document.createElement('canvas');c.width=c.height=size;let x=c.getContext('2d'),img=x.createImageData(size,size),r=(base>>16)&255,g=(base>>8)&255,b=base&255;for(let i=0;i<img.data.length;i+=4){let p=i/4,xx=p%size,yy=(p/size)|0,n=(Math.sin(xx*12.9898+yy*78.233)*43758.5453)%1*noise,grain=((xx*11+yy*5)%29===0?-7:0);img.data[i]=Math.max(0,Math.min(255,r+n+grain));img.data[i+1]=Math.max(0,Math.min(255,g+n+grain));img.data[i+2]=Math.max(0,Math.min(255,b+n+grain));img.data[i+3]=255}x.putImageData(img,0,0);let t=new T.CanvasTexture(c);t.wrapS=t.wrapT=T.RepeatWrapping;t.colorSpace=T.SRGBColorSpace;return t}
let grass=tex(0x698d53,15),asphalt=tex(0x3a3f42,12);grass.repeat.set(70,70);W.ground.material.map=grass;W.ground.material.color.setHex(0xffffff);W.ground.material.roughness=.96;W.ground.material.needsUpdate=true;for(let r of W.roads){let t=asphalt.clone();t.needsUpdate=true;t.repeat.set(Math.max(1,r.mesh.geometry.parameters.width/9),Math.max(1,r.mesh.geometry.parameters.depth/9));r.mesh.material.map=t;r.mesh.material.color.setHex(0xffffff);r.mesh.material.roughness=.92;r.mesh.material.metalness=.01;r.mesh.material.needsUpdate=true}window.LuxSurfaces610={grass,asphalt}},300);