(()=>{const map={KeyA:'d',KeyD:'a',ArrowLeft:'d',ArrowRight:'a'};function editable(){let t=document.activeElement?.tagName;return t==='INPUT'||t==='TEXTAREA'||t==='SELECT'}function apply(e,on){if(!document.body.classList.contains('game-ready')||editable())return;let k=map[e.code]||map[e.key];if(!k)return;let W=window.LuxWorld;if(!W?.keys)return;W.keys[k]=on?1:0;e.preventDefault();e.stopImmediatePropagation()}addEventListener('keydown',e=>apply(e,true),true);addEventListener('keyup',e=>apply(e,false),true);window.LuxControlDirection786=Object.freeze({screenLeft:'physical A / joystick left -> internal d',screenRight:'physical D / joystick right -> internal a',locked:true})})();