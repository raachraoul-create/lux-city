import * as T from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';let q=setInterval(()=>{if(!window.LuxWorld||!window.LuxWeather470)return;clearInterval(q);const W=LuxWorld,S=W.scene,rings=[];for(let i=0;i<22;i++){let m=new T.MeshBasicMaterial({color:0xb9d5e4,transparent:true,opacity:0,depthWrite:false,side:T.DoubleSide}),r=new T.Mesh(new T.RingGeometry(.05,.13,12),m);r.rotation.x=-Math.PI/2;r.userData.life=Math.random();r.visible=false;S.add(r);rings.push(r)}function respawn(r){let a=Math.random()*Math.PI*2,d=2+Math.random()*20;r.position.set(W.player.position.x+Math.cos(a)*d,.16,W.player.position.z+Math.sin(a)*d);r.scale.setScalar(.6);r.userData.life=.35+Math.random()*.3;r.material.opacity=.45;r.visible=true}W.registerTick(dt=>{let rain=LuxWeather470.state==='rain'&&!window.LuxInteriors530?.inside;if(!rain){for(let r of rings)r.visible=false;return}for(let r of rings){r.userData.life-=dt;if(r.userData.life<=0){respawn(r);continue}r.visible=true;let k=1-r.userData.life/.65;r.scale.setScalar(.7+k*2.1);r.material.opacity=Math.max(0,r.userData.life*.72)}});window.LuxWeatherDetail530={rings}},360);