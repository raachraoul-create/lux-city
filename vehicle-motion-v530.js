let q=setInterval(()=>{if(!window.LuxWorld)return;clearInterval(q);const W=LuxWorld,prev=new WeakMap();W.registerTick(dt=>{if(dt<=0||dt>.1)return;let pc=window.LuxPlayerCar471||window.LuxPlayerCar400;for(let v of W.cars||[]){if(v.visible===false)continue;let p=prev.get(v)||{x:v.position.x,z:v.position.z,yaw:v.rotation.y,sp:0},sp=Math.hypot(v.position.x-p.x,v.position.z-p.z)/dt,acc=(sp-p.sp)/dt,dy=v.rotation.y-p.yaw;while(dy>Math.PI)dy-=Math.PI*2;while(dy<-Math.PI)dy+=Math.PI*2;let heavy=v.userData?.bus470||v.userData?.delivery510||v.userData?.garbage510,roll=Math.max(-.035,Math.min(.035,-dy/dt*(heavy?.006:.009))),pitch=Math.max(-.028,Math.min(.028,-acc*(heavy?.0012:.0017)));v.rotation.z+=(roll-v.rotation.z)*Math.min(1,dt*5);v.rotation.x+=(pitch-v.rotation.x)*Math.min(1,dt*5);prev.set(v,{x:v.position.x,z:v.position.z,yaw:v.rotation.y,sp})}let car=pc?.car;if(car&&pc.driving){let p=prev.get(car)||{x:car.position.x,z:car.position.z,yaw:car.rotation.y,sp:0},sp=Math.abs(pc.speed||0),acc=(sp-p.sp)/dt,dy=car.rotation.y-p.yaw;while(dy>Math.PI)dy-=Math.PI*2;while(dy<-Math.PI)dy+=Math.PI*2;car.rotation.z+=(Math.max(-.04,Math.min(.04,-dy/dt*.009))-car.rotation.z)*Math.min(1,dt*6);car.rotation.x+=(Math.max(-.032,Math.min(.032,-acc*.0018))-car.rotation.x)*Math.min(1,dt*6);prev.set(car,{x:car.position.x,z:car.position.z,yaw:car.rotation.y,sp})}});window.LuxVehicleMotion530={active:true}},420);